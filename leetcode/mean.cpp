#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main()
{
    int num1[1000];
    int num2[1000];
    int x, count;
    int y, count1;
    cout << "enter num1\n";
    while (x != 0)
    {
        cin >> num1[x];
        count++;
    }
    cout << "enter num2\n";
    while (y != 0)
    {
        cin >> num1[y];
        count1++;
    }
    for (int i = 0; i < count; i++)
    {
        /* code */
    }
    

    return 0;
}
